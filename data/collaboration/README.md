# Collaboration Data

Workshop session directories are created at runtime under `sessions/`.

```bash
python scripts/collab_workshop.py session-create "My Workshop" --facilitator "Lead Analyst"
```

Imported scenarios from shared sessions are written to `imported/` by default.

See [docs/collaboration-workflows.md](../../docs/collaboration-workflows.md) for the full workflow guide.